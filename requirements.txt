# ---- Broker ----
kiteconnect>=5.0.1

# ---- API server ----
fastapi>=0.115.0
uvicorn[standard]>=0.34.0      # [standard] brings websockets + uvloop + httptools
pydantic>=2.9.0

# ---- Data ----
pandas>=2.2.0
numpy>=2.0.0                   # np.busday_count for the expiry roll

# ---- Auth ----
pyotp>=2.9.0
requests>=2.32.0

# ---- Recorder ----
zstandard>=0.23.0
orjson>=3.10.0                 # fast serialisation on the recorder thread

# ---- Logging / time ----
loguru>=0.7.2
python-dateutil>=2.9.0
tzdata>=2024.1                 # ZoneInfo data on Windows

# ---- Optional ----
# upstox-python-sdk        # required only when data_broker = upstox
# playwright               # required only for unattended Upstox login
#                           (then: playwright install chromium)

# ---- Dev / test ----
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0                  # FastAPI TestClient
